<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eprototype.myhealth">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyHealth">
        <activity
            android:name=".views.CreateKeyPair"
            android:exported="true" />
        <activity
            android:name=".views.CreateFileActivity"
            android:exported="true" />
        <activity
            android:name=".views.EmailVerificationActivity"
            android:exported="true" />
        <activity
            android:name=".views.ProfileActivity"
            android:exported="true" />
        <activity
            android:name=".views.SignInActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.CreateAccountActivity"
            android:exported="true" />
        <activity
            android:name=".views.HomeActivity"
            android:exported="true" />
    </application>

</manifest>